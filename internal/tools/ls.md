List the entries in a directory. Directories are suffixed with `/`. Use this
to orient yourself before reading or globbing.

Input:
- `path` (string, optional): directory to list (default: working directory).
